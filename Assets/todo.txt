- content
    - water
    - trains
    - bunnies
    - magic / qi
    - tofu
    - farming buildings
    - mouse skills
    - books
        - books for gaining skills 
        - manuals for learning technologies
        - scriptoriums
- consider whether itemstacks are useful
- info panel
    - have info panel cycle through when you click it again
- inventory
    - have a "keep" setting that invs can toggle to keep food for consumption and stuff like that. 
- plant
    - flesh out plant growth, moisture and temperature
    - make plants grow from sunlight
- visuals
    - improve mouse animation
    - add icons to inventory panel view / itemdisplay
- ponder inventory management
    - global, market, need to have set target 
    - global can have
        - consume above amount?
- consider stuff thats extraneously saved
- priority
    - add recipe priority
    - add inventory priority? or inventory pull orders
- seems like some floor inventories are getting two stacks for no reason (maybe alert who's dropping whenever this happens?)
- centralize modifiers
    - research multiplier
    - tool productivity multiplier
- research
    - queue research projects, decide what fraction to move onto next project at
    - decide what fraction to maintain researches at?
        - assign scientists to "maintain" duty?
    - lock postrequisites?
- make blueprints / building previews not use normal maps
- item stack reservation is MESSY
    - draw out foolproof spec!
- dynamic objectives!
- FetchObjective partial fill for crafting
    - current: if mouse fetched enough for at least 1 round but can't find more, task fails anyway
    - desired: if can't find more but already have >= 1 round's worth of inputs, skip to crafting
    - approach: give FetchObjective a `minQuantity` alongside the `target` quantity.
      retry across tiles until we have `minQuantity`; once we can't find more and have >= minQuantity, complete.
      for food minQuantity = 1 (fractional eating), for crafting minQuantity = 1 round's ingredient amount.
      this also removes the current special-case `targetInv != null` early-complete — food's minQuantity=1 covers it naturally.


BUGS

TODAY GOALS